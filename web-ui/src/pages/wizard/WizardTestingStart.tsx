import React from 'react';
import { WizardLayout, TestingStart } from '../../components/wizard';

export const WizardTestingStart: React.FC = () => {
  return (
    <WizardLayout>
      <TestingStart />
    </WizardLayout>
  );
};

export default WizardTestingStart;
