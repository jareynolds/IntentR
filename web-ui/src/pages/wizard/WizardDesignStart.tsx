import React from 'react';
import { WizardLayout, DesignStart } from '../../components/wizard';

export const WizardDesignStart: React.FC = () => {
  return (
    <WizardLayout>
      <DesignStart />
    </WizardLayout>
  );
};

export default WizardDesignStart;
