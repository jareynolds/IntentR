import React from 'react';
import { WizardLayout, DefinitionStart } from '../../components/wizard';

export const WizardDefinitionStart: React.FC = () => {
  return (
    <WizardLayout>
      <DefinitionStart />
    </WizardLayout>
  );
};

export default WizardDefinitionStart;
