# Development environment configuration
#
# DEPLOYMENT MODE: Always use nginx as reverse proxy
# Access the application at: http://localhost (nginx on port 80)
#
# This is the same as production - nginx handles all routing.
# Run ./start.sh to start all services including nginx.

VITE_USE_PROXY=true

# Note: Direct service URLs below are NOT used when VITE_USE_PROXY=true
# They are kept for reference only.
# VITE_INTEGRATION_SERVICE_URL=http://localhost:9080
# VITE_DESIGN_SERVICE_URL=http://localhost:9081
# VITE_CAPABILITY_SERVICE_URL=http://localhost:9082
# VITE_AUTH_SERVICE_URL=http://localhost:9083
# VITE_COLLABORATION_SERVER_URL=http://localhost:9084
# VITE_SPECIFICATION_API_URL=http://localhost:4001
# VITE_SHARED_WORKSPACE_URL=http://localhost:4002
